

    <div class="sidebar-menu">
        <!-- استخدام الـ sidebar-item من الـ CSS بتاعك -->
        <a  asp-controller="Admin" asp-action="Index"
           class="sidebar-item @(ViewContext.RouteData.Values["Controller"].ToString() == "Dashboard" ? "active" : "")">
            <span>🏠</span> لوحة التحكم
        </a>

    <a asp-controller="Admin" asp-action="GetCouriers" class="sidebar-item">
            <span>🚴</span> إدارة المناديب
        </a>

    

    <a asp-controller="Admin" asp-action="GetMerchants" class="sidebar-item">
            <span>🏪</span> إدارة التجار
        </a>

    </div>

    <div class="p-3 border-top border-white-10">
        <form asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="~/"
          method="post">
        <button type="submit" class="btn btn-outline-danger w-100 border-0 text-white" style="background: rgba(231, 76, 60, 0.1);">
            تسجيل الخروج
        </button>
    </form>
    </div>
